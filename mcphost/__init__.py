"""FastMCP host exposing lamp tools.

Named ``mcphost`` rather than ``mcp`` so it doesn't shadow the PyPI ``mcp``
package (which we import from for ``FastMCP``). Run via ``python -m mcphost.server``.
"""
